import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: 250, 
        height: 30,
        resizeMode: 'contain',
        alignSelf: 'center',
    }
})

export default styles;
    